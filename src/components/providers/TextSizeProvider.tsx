'use client'

// Font size is applied (and gated) in AppNav to avoid flash on load.
// This file is kept as a no-op so existing imports don't break.
export default function TextSizeProvider() {
  return null
}
